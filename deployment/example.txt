#!/bin/bash

#SBATCH --nodes=1
#SBATCH --ntasks=32
#SBATCH --gres=gpu:2

#   # change allocated time for training, Estimate and err on the upper limit or job will stop in the middle of training
#SBATCH -t 15:00:00

#SBATCH -p v100_norm
#SBATCH -A hauflab
#SBATCH --export=NONE

module purge
module load gcc 
module load Anaconda
module load cuda 
module load mvapich2 
module load openblas

#   # Change the account name to the appropriate ARC user
source activate /home/<UserAccountName>/.conda/envs/testEnv

#   # Change the account name to the appropriate ARC user
#   # Change the file to run training or cross validation
#   # NOTE: This path assumes that the Training Code file is in your ~ directory
#   # example: python /home/nhuuynh18/TrainingCode/run_train.py -n 100 -b 25
python /home/<UserAccountName>/TrainingCode/<train or validation file with arguments>

exit;