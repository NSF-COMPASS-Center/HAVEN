config_type: "prediction" # options: data_preprocessor, prediction, evaluation

# Input Settings:
  ## initialize base input folder name
  ## provide path to dataset folder
input_settings:
  # Base input directory
  input_dir: "input/data/uniref90"

  # Name of file(s) in subdirectory to be read and processed.
  # Example: file_names: ["file_1.csv", "file_2.csv", "file_3.csv"]
  file_names: [ "uniref90_final_msl1114.csv" ]
  # file_names: [ "uniref90_final.csv" ]
  # seeds to split the files into training and testing
  # if provided, the number of seeds must match the number of iterations configured in classification_settings (n_iterations)
  split_seeds: [ 12612648, 31062180, 48918293, 55631155, 79221635 ]


classification_settings:
  model_type: "nlp"
  n_iterations: 5
  split_input: True
  train_proportion: 0.8
  type: "multi"
  sequence_settings:
    batch_size: 64
    id_col: "uniref90_id"
    sequence_col: "seq"
    max_sequence_length: 1115 # 6630 # 1024 # 1115
    truncate: False
    pad_sequence_val: 0
    feature_type: "kmer"  # supported values 'kmer', 'cgr', 'tokens'
    kmer_settings:
      k: 3
    cgr_settings:
      img_size: 64
      img_dir: "input/data/uniref90/cgr_images"
  label_settings:
    label_col: "virus_host_name"
    exclude_labels: [ "nan"]
    label_groupings:
      "Human": [ "Homo sapiens" ]
      "Desert warthog": [ "Phacochoerus aethiopicus" ]
      "Lesser bandicoot rat": [ "Bandicota bengalensis" ]
      "Horse": [ "Equus caballus" ]
      "Goat": [ "Capra hircus" ]
      "Red junglefowl": [ "Gallus gallus" ]
      "Wood mouse": [ "Apodemus sylvaticus" ]
      "Cattle": [ "Bos taurus" ]
      "Others": [ "*" ]
  models:


    - name: "cgr-cnn-l_2"
      active: True
      mode: "train" # supported values: train, test
      loss: "FocalLoss"
      depth: 2
      n_tokens: 27
      n_classes: 9
      n_epochs: 10
      hidden_dim: 1024
      kernel: 3
      stride: 1
      manual_seed:  # whole numbers

output_settings:
  output_dir: "output"
  results_dir: "raw"
  sub_dir: "uniref90/20230712/host_multi_baseline_focal"
  prefix: "emb_sincos-d_1024-loss_focalce" # default: none; default file name = kmer_k<k>_lr_c<c>_<label>_<type>_tr<train_proportion>_n<n_iterations>_<output_prefix>_output.csv
